const express = require('express');
const router = express.Router();

// EXISTING ROUTES
// Use http://auth/api/auth/generateToken to generate a token
// Use http://auth/api/auth/verifyToken to verify a token

// Use data service to write/read to/from the database

// TO WRITE
// Login Route


module.exports = router;